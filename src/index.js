const spawn = require('child_process').spawn

module.exports.bC = {
  split(from) {
    return
  },
  chain(block) {
    return
  },
  join(to) {
    return
  },
  follow(chain) {
    return
  },
  start(name) {
    return
  },
}